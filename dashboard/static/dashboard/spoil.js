window.addEventListener('load', function () {
    var stackButtons = document.getElementsByClassName('content-stack-node-header')
    for (let index = 0; index < stackButtons.length; index++) {
        const element = stackButtons[index];
        element.addEventListener('click', function () {

        })
    }
})